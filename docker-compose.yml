services:
  # --- Postgres HA cluster (repmgr handles replication + automatic failover) ---
  # pg-0 sống trên VM1, pg-1 sống trên VM2 (compose.vm2.yml). Các node tìm nhau
  # qua IP tĩnh của VM + cổng 5432 publish trên mỗi host, KHÔNG qua tên container.
  pg-0:
    image: bitnamilegacy/postgresql-repmgr:16.6.0
    container_name: library_pg0
    env_file:
      - .env
    environment:
      POSTGRESQL_POSTGRES_PASSWORD: ${POSTGRES_ADMIN_PASSWORD}
      POSTGRESQL_USERNAME: ${POSTGRES_USER}
      POSTGRESQL_PASSWORD: ${POSTGRES_PASSWORD}
      POSTGRESQL_DATABASE: ${POSTGRES_DB}
      REPMGR_PASSWORD: ${REPMGR_PASSWORD}
      REPMGR_PRIMARY_HOST: ${VM1_IP}
      REPMGR_PARTNER_NODES: ${VM1_IP},${VM2_IP}
      REPMGR_NODE_NAME: pg-0
      REPMGR_NODE_NETWORK_NAME: ${VM1_IP}
      REPMGR_PORT_NUMBER: 5432
    ports:
      - "5432:5432"          # để pg-1 ở VM2 clone + stream vào
    volumes:
      - pg_0_data:/bitnami/postgresql
    restart: always

  # --- Connection pooler / load balancer in front of the cluster ---
  # The backend (and pgAdmin) only ever talk to pgpool; pgpool always
  # routes writes to whichever node repmgr currently considers primary.
  pgpool:
    image: bitnamilegacy/pgpool:4.6.3
    container_name: library_pgpool
    env_file:
      - .env
    environment:
      PGPOOL_BACKEND_NODES: 0:pg-0:5432,1:${VM2_IP}:5432
      PGPOOL_SR_CHECK_USER: postgres
      PGPOOL_SR_CHECK_PASSWORD: ${POSTGRES_ADMIN_PASSWORD}
      PGPOOL_POSTGRES_USERNAME: postgres
      PGPOOL_POSTGRES_PASSWORD: ${POSTGRES_ADMIN_PASSWORD}
      PGPOOL_ADMIN_USERNAME: admin
      PGPOOL_ADMIN_PASSWORD: ${PGPOOL_ADMIN_PASSWORD}
      PGPOOL_ENABLE_HEALTH_CHECK: "yes"
      PGPOOL_POSTGRES_CUSTOM_USERS: ${POSTGRES_USER}
      PGPOOL_POSTGRES_CUSTOM_PASSWORDS: ${POSTGRES_PASSWORD}
    # Không publish 5432 ra host nữa (pg-0 đã chiếm 5432 trên VM1).
    # Muốn nối bằng GUI từ máy Windows thì mở dòng dưới (cổng 5433):
    # ports:
    #   - "5433:5432"
    depends_on:
      - pg-0
    healthcheck:
      test: ["CMD-SHELL", "PGPASSWORD=${POSTGRES_PASSWORD} psql -h 127.0.0.1 -p 5432 -U ${POSTGRES_USER} -d ${POSTGRES_DB} -tAc 'SELECT 1'"]
      interval: 5s
      timeout: 5s
      retries: 10
      start_period: 30s
    restart: always

  pgadmin:
    image: dpage/pgadmin4:snapshot
    container_name: library_pgadmin
    restart: always
    env_file:
      - .env
    ports:
      - "5050:80"
    volumes:
      - pgadmin_data:/var/lib/pgadmin
    depends_on:
      - pgpool

  # --- Backup định kỳ: mỗi 6h dump DB ra thư mục trên máy thật (Windows) ---
  # Ghi thẳng qua SMB nhờ volume `host_pg_backups` (CIFS) khai báo bên dưới.
  pg-backup:
    build:
      context: ./deploy/backup
    image: library-pg-backup:latest
    container_name: library_pg_backup
    env_file:
      - .env
    environment:
      POSTGRES_DB: ${POSTGRES_DB}
      POSTGRES_ADMIN_PASSWORD: ${POSTGRES_ADMIN_PASSWORD}
      # pg-0 qua docker network, pg-1 qua IP VM2. Script ưu tiên dump từ primary.
      PG_NODES: "pg-0:5432 ${VM2_IP}:5432"
      BACKUP_INTERVAL: "21600"   # 6 giờ
      KEEP: "28"                 # giữ 28 bản .dump gần nhất
      TZ: "Asia/Ho_Chi_Minh"
    volumes:
      - host_pg_backups:/backups
    depends_on:
      - pg-0
    restart: always

  backend:
    build:
      context: ./backend_library
      dockerfile: Dockerfile
    container_name: library_backend
    env_file:
      - .env
    environment:
      # Points the backend at pgpool instead of a single Postgres node,
      # so a primary failover is invisible to the app.
      POSTGRES_HOST: pgpool
      POSTGRES_PORT: 5432
    ports:
      - "8000:8000"
    restart: always
    depends_on:
      pgpool:
        condition: service_healthy

  frontend:
    build:
      context: ./frontend_library
      dockerfile: Dockerfile
      args:
        NEXT_PUBLIC_API_URL: ${NEXT_PUBLIC_API_URL}
    container_name: library_frontend
    ports:
      - "3000:3000"
    environment:
      NEXT_PUBLIC_API_URL: ${NEXT_PUBLIC_API_URL}
    depends_on:
      - backend
    restart: always

volumes:
  pg_0_data:
  pgadmin_data:

  # Thư mục trên máy thật Windows, chia sẻ qua SMB.
  # Trên Windows: share thư mục C:\Users\doant\OneDrive\Documents\Desktop\pg-backups
  #   với Share name = pg-backups, cấp Change + Read cho user SMB_USERNAME.
  # WIN_HOST_IP / SMB_USERNAME / SMB_PASSWORD lấy từ .env
  host_pg_backups:
    driver: local
    driver_opts:
      type: cifs
      device: "//${WIN_HOST_IP}/pg-backups"
      o: "username=${SMB_USERNAME},password=${SMB_PASSWORD},uid=0,gid=0,vers=3.0,file_mode=0640,dir_mode=0750,noserverino"
